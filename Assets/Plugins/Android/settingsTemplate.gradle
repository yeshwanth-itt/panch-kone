pluginManagement {
    repositories {
        **ARTIFACTORYREPOSITORY**
        gradlePluginPortal()
        google()
		mavenCentral()
        maven { 
            url = "https://plugins.gradle.org/m2/org/ow2/asm/asm/9.2/";
			url = "https://plugins.gradle.org/m2/org/jetbrains/dokka/dokka-core";
            setAllowInsecureProtocol(true);
	    } 
    }
}

include ':launcher', ':unityLibrary'
**INCLUDES**

dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.PREFER_SETTINGS)
    repositories {
        **ARTIFACTORYREPOSITORY**
        google()
        maven { 
            url = "https://plugins.gradle.org/m2/org/ow2/asm/asm/9.2/";
			url = "https://plugins.gradle.org/m2/org/jetbrains/dokka/dokka-core";
            setAllowInsecureProtocol(true);
	   }
	   mavenCentral()
        flatDir {
            dirs "${project(':unityLibrary').projectDir}/libs"
        }
    }
}
